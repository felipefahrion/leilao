@model IEnumerable<LeilaoDoMeuCoracao.PL.Leilao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataInicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataMaxLances)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Valor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StatusLeilaoEnum)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataInicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataMaxLances)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StatusLeilaoEnum)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LeilaoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LeilaoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LeilaoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
